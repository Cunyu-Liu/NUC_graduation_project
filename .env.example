# ============================================================================
# 环境变量配置示例文件 - v4.1
# ============================================================================
# 使用说明：
# 1. 复制此文件为 .env
# 2. 填入真实值（特别是标有"必需"的配置）
# 3. 确保 .env 文件不被提交到Git仓库

# ============================================================================
# 数据库配置（必需）
# ============================================================================
# PostgreSQL数据库连接URL
# 格式: postgresql://username:password@localhost:5432/database_name
# 请先创建数据库: createdb literature_analysis
DATABASE_URL=postgresql://user:password@localhost:5432/literature_analysis

# ============================================================================
# GLM-4 API配置（必需）
# ============================================================================
# 智谱AI API密钥 - 从 https://open.bigmodel.cn/ 获取
# 注册并创建API密钥
GLM_API_KEY=your_glm_api_key_here

# API基础URL（默认，通常不需要修改）
GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4

# 默认模型配置（可选）
DEFAULT_MODEL=glm-4-flash
DEFAULT_TEMPERATURE=0.3
MAX_TOKENS=4000

# ============================================================================
# Flask配置（可选）
# ============================================================================
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=False

# ============================================================================
# 输出目录配置（可选）
# ============================================================================
OUTPUT_DIR=./output
SUMMARY_OUTPUT_DIR=./output/summaries
KEYPOINTS_OUTPUT_DIR=./output/keypoints
CLUSTER_OUTPUT_DIR=./output/clusters

# ============================================================================
# Redis配置（可选，用于缓存）
# ============================================================================
# 如果没有Redis，可以不配置，系统会使用内存缓存
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# ============================================================================
# 其他配置（可选）
# ============================================================================
# 最大并发数（同时处理的论文数）
MAX_CONCURRENT=5

# 日志级别（DEBUG, INFO, WARNING, ERROR）
LOG_LEVEL=INFO

# 前端WebSocket URL（可选）
# VITE_WS_URL=ws://localhost:5000
